<?xml version="1.0" encoding="UTF-8"?>
<?xml-model href="http://www.tei-c.org/release/xml/tei/custom/schema/relaxng/tei_all.rng" type="application/xml" schematypens="http://relaxng.org/ns/structure/1.0"?>
<?xml-model href="http://www.tei-c.org/release/xml/tei/custom/schema/relaxng/tei_all.rng" type="application/xml" schematypens="http://purl.oclc.org/dsdl/schematron"?>
<TEI xmlns="http://www.tei-c.org/ns/1.0">
   <teiHeader>
      <fileDesc>
         <titleStmt>
            <title type="main">Erwähnte und verzeichnete Briefe</title>
            <title type="sub">Korrespondenz von Kaiser Leopold I</title>
            <editor key="https://d-nb.info/gnd/132150654">
                <forename>Petr</forename>
                <surname>Maťa</surname>
            </editor>
        </titleStmt>
        <publicationStmt>
            <publisher key="https://d-nb.info/gnd/1001454-8">Österreichische Akademie der Wissenschaften</publisher>
            <date when="2028"/>
            <availability status="free">
               <licence target="https://creativecommons.org/licenses/by/4.0/deed.de">Lizenziert unter CC-BY-4.0 (https://creativecommons.org/licenses/by/4.0/deed.de)</licence>
            </availability>
        </publicationStmt>
         <sourceDesc>
            <p>Register der im Projekt erwähnten Briefe</p>
         </sourceDesc>
      </fileDesc>
      <profileDesc>{% for x in objects %}
         <correspDesc xml:id="{{ x.lb_id }}">
            <correspAction type="sent">
               <persName key="lb-person-1" ref="https://d-nb.info/gnd/118571869">Leopold I.</persName>
           {% if x.place_of_writing or x.placename_written %}
               {% for place in x.place_of_writing %}
               <placeName key="{{ place.lb_id }}"{% if place.geonames_url %} ref="{{ place.geonames_url }}"{% endif %}>{{ place.label }}</placeName>
               {% endfor %}
               {% if x.placename_written %}
               <placeName type="written">{{ x.placename_written }}</placeName>
               {% endif %}
            {% endif %}
            {% if x.correct_date %}
            <date when="{{ x.correct_date }}">{{ x.correct_date }}</date>
            {% else %}
               <date when="{{ x.not_before }}">{{ x.not_before }}</date>
            {% endif %}
            </correspAction>
            <correspAction type="received">
            {% for receiver in x.addressee_worked_out %}
               <persName type="addressee_worked_out" key="{{ receiver.lb_id }}"{% if receiver.gnd_url %} ref="{{ receiver.gnd_url }}"{% endif %}>{{ receiver.label }}</persName>
            {% endfor %}
            {% for receiver in x.addressee_stated %}
               <persName type="addressee_stated" key="{{ receiver.lb_id }}"{% if receiver.gnd_url %} ref="{{ receiver.gnd_url }}"{% endif %}>{{ receiver.label }}</persName>
            {% endfor %}
            {% if x.destination %}
            {% for place in x.destination %}
               <placeName type="destination" key="{{ place.lb_id }}"{% if place.geonames_url %} ref="{{ place.geonames_url }}"{% endif %}>{{ place.label }}</placeName>
               {% endfor %}
            {% endif %}
            {% if x.received_place %}
               {% for place in x.received_place %}
               <placeName type="received_place" key="{{ place.lb_id }}"{% if place.geonames_url %} ref="{{ place.geonames_url }}"{% endif %}>{{ place.label }}</placeName>
               {% endfor %}
            {% endif %}
            {% if x.received_date %}
               <date when="{{ x.received_date }}">{{ x.received_date }}</date>
            {% endif %}
            </correspAction>{% if x.note %}
            <note type="anmerkung">{{ x.note }}</note>{% endif %}
         </correspDesc>{% endfor %}
      </profileDesc>
   </teiHeader>
   <text>
      <body>
         <ab></ab>
      </body>
   </text>
</TEI>
